 git subtree push --prefix=doc/_build/html origin gh-page
sphinx-build -b html . _build/html

Manual updating of documentation:
   git checkout gh-pages
   cp -r _build/html/* .
   git add .
   git commit -m "Update documentation with code block fixes"
   git push origin gh-pages
